{{! mustache equivalent of renderfield macro from forms.html }}
{{! example usage: mustache('baseframe/renderfield.mustache', partials={'rendererrors':'baseframe/rendererrors.mustache'}, field=formfield|to_json) }}

{{#field}}
<div class="mui-form__controls {{#errors}}error{{/errors}} {{css_class}}" {{#is_listwidget}} listwidget {{/is_listwidget}} id="field-{{id}}">

    {{#is_checkbox}}
    <div class="mui-checkbox"> {{label}}{{render_html}}
    {{/is_checkbox}}

    {{^is_checkbox}}
    <div class="mui-textfield mui-textfield--float-label">
        {{ render_html }}
        <label for="{{id}}">{{label}}{{#is_required}} <span class="mui-form__label" title="Required">*</span>{{/is_required}}</label>
    {{/is_checkbox}}

        {{# sidetext }}
            <span class="mui-form__sidetext">{{ sidetext }}</span>
        {{/ sidetext}}
        {{ > rendererrors }}
        {{# description}}
            <p class="mui-form__helptext">{{ description }}</p>
        {{/ description}}
    </div>
</div>
{{/field}}
